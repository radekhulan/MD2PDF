# PHP deps (mPDF) — obnov přes `composer install`
vendor/

# Node deps (mermaid-cli + Chromium) — obnov přes `npm install`
node_modules/
package-lock.json

# Prohlížeč stažený puppeteerem (volitelné; lze i systémový Chrome/Edge)
.puppeteer/

# Mermaid render cache
.mermaid-cache/
pdf/_preview

# Editor / nástroje / AI onboarding (interní — odkazují na privátní vault)
.source/
.vscode/
.claude/
AGENTS.md
CLAUDE.md

# Lokální / dočasné
*.tmp
COMMIT_MSG.tmp
publish.cmd